# dependencies
node_modules/
.pnpm-store/

# next.js
.next/
out/
next-env.d.ts

# prisma
prisma/*.db
prisma/*.db-journal

# uploads (runtime volume at repo root only — do not use bare `uploads/`, it ignores `lib/uploads/`)
/uploads/

# env
.env
.env.local
.env.*.local

# misc
*.log
.DS_Store
.turbo
coverage/
*.tsbuildinfo

# playwright MCP scratch
.playwright-mcp/

# Playwright (CLI)
playwright-report/
test-results/

# ci-e2e dumps
test-artifacts/
